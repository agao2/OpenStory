@model OpenStory.Models.PostPartialViewModel

<a href="" class="tight">
    <div class="row ">
        <div class="col-sm-1">
            <h4>@Model.Username</h4>
        </div>
        <div class="col-sm-1">

        </div>
        <div class="col-sm-10">
            <div class=" list-group">
                <h4 class="list-group-item-heading">@Model.Title</h4>
                <p class="list-group-item-heading">@Model.PostDate</p>
                <p class="list-group-item-heading"> @Model.Content</p>
            </div>
        </div>

    </div>
    <hr class="tight" />
</a>


@*"@Url.Action("Topic/" + Model.Story.Id, "Stories")"*@